# Node/React
node_modules
.pnpm-store
dist
build
.next
.cache
.vite
.DS_Store

# Env & local config
.env
.env.*
!.env.example

# Claude Code
.claude/worktrees/

# BuildLoop orchestrator state (per-iteration artifacts not committed)
# Note: docs/features/SHIPPED.md IS committed — that's the public ship log.
.buildloop/

# Office lock files
.~lock.*

# Replit
.replit
.replit-nix
*.replit
