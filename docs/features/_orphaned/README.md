# Orphaned tickets

Tickets that did not have a clear parent epic in Jira at port time. Review each and either:

1. Move to a feature folder (update `feature:` and `epic:` frontmatter)
2. Move to `_review.md` as a stale candidate
3. Delete if no longer relevant

Once empty, delete this folder.
